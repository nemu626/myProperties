colorscheme molokai

set number "Line Number
set cursorline "highlight cursor line
set showmatch "highlight matching brackets

set scrolloff=8 "always can view 8 line up and down

set confirm "confirm when unsaved File closed
"search
set hlsearch "highlight search word
set incsearch "do incremental search
set ignorecase "ignode UPPER and lower case
set smartcase "do not ignore uPPer and loWer case when mixed.

set expandtab "change tab to whitespace
set tabstop=2 "tab width
set shiftwidth=2 "autoindent whitespace width
set softtabstop=2 
set autoindent
set smartindent

set mouse=a "enable mouse
set clipboard=unnamed,unnamedplus


 
